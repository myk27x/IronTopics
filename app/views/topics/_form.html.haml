= form_for(@topic) do |f|
  #add_field
    .field
      = f.label :focus_area
      %br
      = f.text_field :focus
    .field
      = f.label :title
      %br
      = f.text_field :title
    .field
      = f.label :description
      %br
      = f.text_area :description
      .actions
        = f.submit
        #cancel
          = link_to "Cancel", root_path
