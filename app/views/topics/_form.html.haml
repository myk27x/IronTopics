= form_for(@topic) do |f|
  #add_field
    .field
      = f.text_field :focus, placeholder: "Focus area"
    .field
      %br
      = f.text_field :title, placeholder: "Title"
    .field
      %br
      = f.text_area :description, placeholder: "Enter your description here"
      .actions
        = f.submit
        #cancel
          = link_to "Cancel", root_path
