= form_for(@topic) do |f|
  #add_field
    = f.text_field :title, placeholder: " Title"
    = f.text_field :description, placeholder: " Description"
    = f.text_field :focus, placeholder: " Focus area"
    #topic_submit= f.submit
